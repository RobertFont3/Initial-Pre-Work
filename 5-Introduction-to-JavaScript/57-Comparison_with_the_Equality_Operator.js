//the equality operates by compairing two values instead of = which applies the value of everything on the right to the left of the symbol


// Setup
function testEqual(val) {
  if (val == 12) {
    return "Equal";
  }
  return "Not Equal";
}
