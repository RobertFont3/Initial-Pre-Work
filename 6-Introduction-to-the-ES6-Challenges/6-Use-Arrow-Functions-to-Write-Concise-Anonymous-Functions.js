/*used arrow function to eliminate the need for a name of a variable that will still return a value*/

const magic = () => new Date();

/*when there is no function name this will eliminate the need for brackets and return*/
